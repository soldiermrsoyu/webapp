<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pasukan Keong - SPV</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <div class="header-section">
      <div>
        <h1 class="brand-title"><i class="fas fa-shell"></i> Pasukan Keong</h1>
        <p class="welcome-msg">Halo, <strong><%= user.user %></strong> (SPV)</p>
      </div>
      <div class="header-actions">
        <div class="notification-badge">
          <i class="fas fa-bell"></i>
          <span class="dot"></span>
        </div>
      </div>
    </div>
    
    <div class="menu-grid">
      <div class="menu-card modern" onclick="location.href='/spv/task'">
        <div class="menu-icon-wrapper blue">
          <i class="fas fa-tasks"></i>
        </div>
        <div class="menu-info">
          <div class="menu-title">Jadwal Hari ini</div>
          <div class="menu-desc">Kelola jadwal dan penugasan</div>
        </div>
      </div>
      <div class="menu-card modern" onclick="location.href='/spv/rencana'">
        <div class="menu-icon-wrapper orange">
          <i class="fas fa-calendar-check"></i>
        </div>
        <div class="menu-info">
          <div class="menu-title">Progres Pekerjaan</div>
          <div class="menu-desc">Daftar tugas teknisi</div>
        </div>
      </div>
      <div class="menu-card modern" onclick="location.href='/spv/laporan'">
        <div class="menu-icon-wrapper green">
          <i class="fas fa-chart-line"></i>
        </div>
        <div class="menu-info">
          <div class="menu-title">Laporan</div>
          <div class="menu-desc">Hasil kerja teknisi</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bottom Navigation -->
  <nav class="bottom-nav modern">
    <a href="/spv" class="nav-item active" id="home-nav">
      <div class="nav-icon"><i class="fas fa-home"></i></div>
      <div class="nav-label">Home</div>
    </a>
    <a href="/spv/notifikasi" class="nav-item" id="notif-nav">
      <div class="nav-icon"><i class="fas fa-bell"></i></div>
      <div class="nav-label">Notif</div>
    </a>
    <a href="/spv/profile" class="nav-item" id="profile-nav">
      <div class="nav-icon"><i class="fas fa-user-circle"></i></div>
      <div class="nav-label">Profil</div>
    </a>
  </nav>

  <script>
    // Set active nav item based on current page
    const currentPage = window.location.pathname;
    document.querySelectorAll('.nav-item').forEach(item => {
      item.classList.remove('active');
    });
    
    if (currentPage === '/spv') {
      document.getElementById('home-nav')?.classList.add('active');
    } else if (currentPage === '/spv/notifikasi') {
      document.getElementById('notif-nav')?.classList.add('active');
    } else if (currentPage === '/spv/profile') {
      document.getElementById('profile-nav')?.classList.add('active');
    }
  </script>
</body>
</html>
